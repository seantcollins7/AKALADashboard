# AWS Configuration (OPTIONAL - only needed if using AWS credentials for database access)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_SESSION_TOKEN=your_aws_session_token

# Database Configuration (REQUIRED)
DB_HOST=your_rds_endpoint.amazonaws.com
DB_PORT=5432
DB_NAME=your_database_name
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password
DB_ENGINE=postgresql

# Google Looker Studio Configuration (OPTIONAL - add when Google integration is set up)
# GOOGLE_CREDENTIALS_FILE=path/to/service-account.json
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}
# GOOGLE_SPREADSHEET_ID=your_existing_spreadsheet_id_if_any
# GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id

# Application Configuration
LOG_LEVEL=INFO
CACHE_TIMEOUT=3600
